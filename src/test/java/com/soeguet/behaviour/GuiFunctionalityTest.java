package com.soeguet.behaviour;

import com.soeguet.gui.main_frame.MainFrameInterface;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import static org.junit.jupiter.api.Assertions.*;

class GuiFunctionalityTest {

    @Test
    void onMessage() {

//        MainFrameInterface mainFrame = Mockito.spy(MainFrameInterface.class);
//        GuiFunctionality guiFunctionality = new GuiFunctionality(mainFrame);
//        guiFunctionality.onMessage("test");
    }
}